let val
const list = document.querySelector('ul.collection');
const listArray = document.querySelector('li.collection-item:first-child')

val = list;
// returns node list 
let listArr = Array.from(list.childNodes);

 val = listArray;

console.log(list.children);

let listChild = Array.from(list.children);

